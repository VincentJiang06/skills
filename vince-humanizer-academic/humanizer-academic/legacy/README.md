# Legacy (retired packaging)

`openai.yaml` is the **retired Codex/OpenAI packaging** of this skill. It is NO
longer the entry point. The skill is now a Claude Code skill defined by the
top-level `SKILL.md` (frontmatter `name` + `description` + `allowed-tools`).

Kept only as a rollback reference for the v1.x → v2.0.0 cutover (see CHANGELOG.md).
Do not load it as part of normal operation.
