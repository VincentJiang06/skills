# Harness & Tooling Layer / Harness 与工具层

machine_summary_zh: 循环的具体构件（Osmani 五件+记忆）：定时自动化、git worktree、skills、连接器、子 agent，加一处外部状态记忆。hooks 把建议变强制；OpenAI 把 CDP 接入运行时按 worktree 启实例做修复-重启-复验。skills 作为知识记忆需审计安全。

machine_summary_en: The concrete building blocks of a loop (Osmani's five + memory): scheduled automation, git worktrees, skills, connectors, subagents, plus one external state-memory store. Hooks turn advisory into enforced; OpenAI wired CDP into the runtime and boots one instance per worktree for fix-restart-revalidate. Skills as knowledge memory must be security-audited.

reference_ids: `ref.osmani.loop_engineering`, `ref.anthropic.claude_code_hooks`, `ref.anthropic.claude_code_best_practices`, `ref.openai.harness_engineering`, `ref.anthropic.agent_skills`, `ref.oreilly.agent_harness_engineering`, `ref.fable.loop_engineering_synthesis_2026_07`, `ref.anthropic.effective_harnesses_long_running`, `ref.huntley.ralph_loop`, `ref.anthropic.agent_sdk_budget_caps`

node_ids: `concept.loop_primitives`, `technique.scheduled_automation`, `technique.git_worktree_isolation`, `technique.hooks_as_deterministic_gate`, `technique.subagent_split`, `concept.skill_as_memory`, `concept.external_state_memory`, `concept.connectors_mcp`, `pattern.cdp_per_worktree_loop`, `principle.effort_routing`

## 1. The five primitives + memory ✓✓

Osmani's "five pieces and then one place to remember stuff" (his taxonomy, not yet a vendor-neutral standard):

1. **Scheduled automations** — fire on a timer, do discovery + triage themselves (`technique.scheduled_automation`).
2. **Git worktrees** — so parallel agents don't step on each other (`technique.git_worktree_isolation`).
3. **Skills** — write down project knowledge so it persists (`concept.skill_as_memory`).
4. **Plugins / connectors** — plug the agent into tools you already use: Jira, GitHub, Slack (`concept.connectors_mcp`).
5. **Sub-agents** — "one has the idea and a different one checks it" (`technique.subagent_split`).
6. **External memory** — "a markdown file, or a Linear board… that lives outside the single conversation and holds what's done and what is next" (`concept.external_state_memory`).

## 2. Hooks — the deterministic glue ✓✓

Claude Code hooks fire shell / HTTP / LLM at lifecycle points — session, turn, and *around each tool call inside the agentic loop* ([hooks](https://code.claude.com/docs/en/hooks)). They turn "please remember to run tests" (advisory, ignorable) into "the turn cannot end until tests pass" (enforced by the harness). A Stop hook with exit code 2 prevents stopping — the mechanism behind `procedure.stop_gate`.

## 3. OpenAI's harness in practice ✓✓

Architectural constraints enforced *mechanically* via (1) custom linters *themselves generated by Codex*, (2) structural tests validating dependency directions, (3) CI jobs blocking boundary-violating PRs; the loop runs until tests/types/builds pass. They wired the **Chrome DevTools Protocol** into the runtime, booting *one app instance per git worktree*, looping *fix → restart → revalidate until the app is clean* (`pattern.cdp_per_worktree_loop`).

> Minor nuance: OpenAI's "Ralph Wiggum Loop" label attaches to their PR-self-review loop, not specifically the CDP UI loop — both are iterative act/check/fix loops.

## 4. Skills as memory — with a security caveat

Anthropic frames capturing successful methods + common errors into reusable context + code (skills) as the loop's knowledge memory. Caveat: skills contain instructions *and code*, can introduce vulnerabilities or exfiltrate data → **install only from trusted sources and audit them** (`anti_pattern.permission_blast_radius`).

Two memory tiers: **knowledge memory** (skills / AGENTS.md / CLAUDE.md / rules) and **state memory** (the external done/next store). The latter is the engineering answer to context drift (`anti_pattern.context_drift`) and the descendant of Reflexion's episodic memory buffer.

### 4a. Externalized-memory recipes in the wild ◐

The state-memory tier has two well-documented 2026 realizations, both trading in-context compaction for on-disk state:

- **Anthropic's long-running harness recipe** (`ref.anthropic.effective_harnesses_long_running`, tier-1, primary): an *initializer* agent sets up the repo once, then a *coding* agent works incrementally across many **fresh** sessions; persistent state lives in a comprehensive feature-list JSON (each item `passes:false` with per-item e2e verification steps), a `claude-progress.txt` log, and git history. Fixed startup: verify dir → read git log + progress → pick highest-priority incomplete feature → start dev server → run basic e2e *before* new work. Verbatim guardrail: *"It is unacceptable to remove or edit tests"* — a first-party anti-reward-hacking rule. Anthropic states single-agent vs specialized-multi-agent remains **open**.
- **The Ralph loop** (`ref.huntley.ralph_loop`, tier-3, Huntley): `while :; do cat PROMPT.md | claude-code; done` — each iteration is a genuinely fresh session and *all* memory is externalized to `PROMPT.md` + `specs/` + a mutating `fix_plan.md`. A named counterpoint to compaction-based survival; its anecdotal cost/token figures are deliberately not ingested.

### 4b. Effort routing ◐

Model-synthesized, owner-authorized (`ref.fable.loop_engineering_synthesis_2026_07`, tier-2): route model tier and reasoning effort by stage role — mechanical stages (formatting, scaffolding, bulk edits) run cheap and low; adversarial verification and judging run at the highest tier affordable. Uniform effort both burns budget and under-verifies; money saved on the generator belongs on the evaluator (`principle.effort_routing`). It is the first lever on `metric.cost_per_resolution`, ahead of caching or batching. Relatedly, the Claude Agent SDK's `max_turns` / `max_budget_usd` **default to unlimited** (`ref.anthropic.agent_sdk_budget_caps`) — budget-capping is opt-in, so any SDK-based runbook must wire it explicitly. *算力按角色路由：机械阶段用便宜低档，对抗验证用最高档；SDK 预算默认无限，须显式接线。*

See also: `doc.governance.stop_conditions_and_multi_agent` (subagents → maker/checker, worktrees → isolation), `doc.risks.costs_risks_failure_modes` (subagents → token blowup; skills → security).
