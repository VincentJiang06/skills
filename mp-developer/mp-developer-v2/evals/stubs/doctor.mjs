// RED stub — wrong sentinels so the doctor cases fail their assertions before the real doctor.mjs.
export function resolveProject() { return { ok: true, reason: null, mpRoot: '/__stub', appid: null } }
export function checkFreshness() { return { status: 'ok', detail: '__stub' } }
