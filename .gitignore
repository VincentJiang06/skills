# OS / editor cruft
.DS_Store
__pycache__/
*.pyc

# Machine-local Claude Code config (never publish)
.claude/settings.local.json

# Internal research notes (never publish)
.research/

# skill pipeline build provenance (not part of the shipped skill)
.skill-conductor/
.skill-guidance/
.skill-engineer/
.tdd-red/

# Generated sample outputs and run logs
output/
*.log

# Node deps (tools/vince-mp-cli)
node_modules/

# Reference library (cloned for study only) and local mini-program test fixture
learning_from_the_best/
miniprogram_test/

# ─── Local-only: eval/test fixtures, dev harnesses & internal notes ───
# GitHub ships the minimum RUNNABLE set per skill (SKILL.md + references/ rules/
# scripts/ schemas/ assets/ agents/). Full eval suites stay on disk for dev.
skills/*/evals/
skills/*/tests/
skills/*/meta/
skills/*/legacy/
/evals/          # repo-root debug harnesses (e.g. skill-pipeline-debug) stay on disk
skills/mp-groundline/scripts/validate-skill.mjs
/experiments/
/docs/
/tools/vince-mp-cli/test/
/tools/vince-mp-cli/tests/
